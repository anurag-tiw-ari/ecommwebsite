export function updateCartValue(arr)
{
   document.querySelector(".fa-cart-shopping").innerText=arr.length
  // console.log(arr)
   /*let id=[]
   for(let i=0;i<arr.length;i++)
    {
        id.push(arr[i].id)
    }
   // console.log(id)

    let idSet=new Set(id)
    console.log()
    let idsetarr=[...idSet]
    document.querySelector(".fa-cart-shopping").innerText=idsetarr.length*/
}